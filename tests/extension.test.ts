import * as vscode from 'vscode'
import * as extension from '../src/extension'

describe('extension', () => {
  // Activate the extension before each test.
  beforeEach(extension)

  test('saves a file record when the file has changed', () => {

  })

  test('does not save a file record when the file has not changed', () => {

  })
})
